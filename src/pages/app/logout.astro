---
export const prerender = false;
if (Astro.request.method !== 'POST') {
  return Astro.redirect('/app/login');
}

Astro.cookies.delete('sb-session', { path: '/' });
return Astro.redirect('/app/login');
---
export const prerender = false;
